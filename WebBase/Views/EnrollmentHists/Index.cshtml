@model IEnumerable<ContosoUniversity.Models.EnrollmentHist>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.enrollments)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.course)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.enrollments)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EnrollmentHistID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EnrollmentHistID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EnrollmentHistID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
